# App: set PORT to run health HTTP server (GET /health, GET /status). Default 3000 when PORT set.
# PORT=3000
# N8N_URL=http://localhost:5678   # optional; used by GET /status to ping n8n

# MCP (Cursor / Claude Code) — set before starting Cursor so ${env:VAR} in .cursor/mcp.json works
NOTION_TOKEN=
GITHUB_PERSONAL_ACCESS_TOKEN=
LINEAR_API_KEY=
LINEAR_TEAM_ID=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional for GitHub command center actions
# GITHUB_OWNER=iurii-izman
# GITHUB_REPO=AIPipeline
# GITHUB_WORKFLOW_STAGING=deploy-staging.yml
# GITHUB_WORKFLOW_PRODUCTION=deploy-production.yml

# n8n (when running via script; or set in n8n UI Credentials)
# N8N_BASIC_AUTH_USER=
# N8N_BASIC_AUTH_PASSWORD=
# N8N_API_KEY=   # optional: for calling n8n API from scripts (keyring: server n8n, user aipipeline-api)

# Optional: Sentry DSN (for app and n8n webhooks)
# SENTRY_DSN=
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG_SLUG=
# SENTRY_PROJECT_SLUG=

# Optional: LLM classification for WF-3
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Notion: after creating Delivery Hub root page and sharing with integration, set for scripts/notion-create-delivery-hub-structure.sh
# NOTION_DELIVERY_HUB_PAGE_ID=
# NOTION_SPRINT_LOG_DATABASE_ID=

# Copy to .env and fill; never commit .env
