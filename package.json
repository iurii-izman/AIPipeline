{
  "name": "aipipeline",
  "version": "0.1.0",
  "description": "AI-native delivery pipeline scaffold",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "lint": "node -e \"require('./src/index')\"",
    "build": "node -e \"require('./src/index')\"",
    "test": "node tests/smoke.test.js && node tests/health.test.js",
    "status": "./scripts/health-check-env.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@sentry/node": "^10.40.0"
  }
}
